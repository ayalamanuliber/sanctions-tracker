export default function Loading() {
  return <main style={{ minHeight: "70vh", display: "grid", placeItems: "center", background: "#f7f9fc", color: "#183451" }}><div role="status" aria-live="polite"><strong>Loading AI Vortex evidence…</strong><p style={{ color: "#64748b", marginTop: 8 }}>Preparing the requested public record.</p></div></main>;
}
